# Secrets — 絕對不能進 repo
.env
.env.*
!.env.example
*.key
*.pem
secrets/
**/credentials.json
**/credentials/

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Node
node_modules/
package-lock.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store
desktop.ini

# Build / 暫存
build/
dist/
*.log
*.tmp

# 個人筆記(不該分享的私人 memo)
private-notes.txt
private-notes.md
TODO_personal.md

# Manifest snapshot 不進 repo(留在 D:\Recovery\)
snapshot-*/

# Claude Code per-project local state (per-user, never share)
.claude/*
# 例外:skills/ 入 repo(可分享的 skill 定義)
!.claude/skills/

# Progress reports — 本地執行端任務暫存,內容會被整合進對應 MD 後刪除
# (README.md 例外保留,讓目錄結構可見)
progress-reports/*
!progress-reports/README.md

# Assignments — session 1 寫的派工,session 2 讀完跑完進 progress-reports 後刪檔
# (README.md 例外保留,讓目錄結構可見)
assignments/*
!assignments/README.md
!progress-reports/session1-snapshot.md